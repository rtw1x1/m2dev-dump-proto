cmake_minimum_required(VERSION 3.10 FATAL_ERROR)

project(m2dev-dump-proto)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

include_directories("lzo-2.10/include/lzo")

# This adds the LZO library project
add_subdirectory(lzo-2.10)

# This adds your dump_proto executable project
add_subdirectory(dump_proto)